# GameModel 重构说明

## 重构前的问题

原来的 `GameModel` 类承担了太多职责，违反了单一职责原则：

1. **实体生成管理** - 平台、敌人、豆子的生成逻辑
2. **实体更新管理** - 所有实体的更新循环  
3. **碰撞检测** - 各种实体间的碰撞逻辑
4. **技能系统** - 技能的初始化和使用
5. **游戏状态管理** - 分数、时间、游戏结束条件
6. **玩家控制接口** - 玩家操作的转发

这导致了：
- 代码难以维护
- 功能耦合严重
- 单个类过于庞大（400+ 行）
- 难以进行单元测试
- 违反开闭原则

## 重构后的架构

### 新增的管理器类

#### 1. EntityManager (实体管理器)
**职责**: 管理所有游戏实体的生命周期
- 统一管理平台、敌人、子弹、豆子、箭矢、技能
- 处理实体的更新循环
- 自动清理超出窗口的实体
- 提供统一的实体访问接口

#### 2. GeneratorManager (生成器管理器)
**职责**: 负责游戏实体的生成逻辑
- 管理各种实体的生成间隔
- 实现不同类型实体的生成算法
- 处理初始化时的实体生成
- 支持动态调整生成参数

#### 3. CollisionManager (碰撞管理器)
**职责**: 处理所有碰撞检测逻辑
- 玩家与子弹的碰撞检测
- 玩家与豆子的碰撞检测
- 箭矢与敌人的碰撞检测
- 返回碰撞结果和分数变化

#### 4. SkillManager (技能管理器)
**职责**: 管理技能系统
- 技能的初始化和冷却管理
- 技能效果的实现
- 箭矢发射和冲刺逻辑
- 技能与实体系统的交互

### 重构后的 GameModel

现在的 `GameModel` 变得更加简洁，只负责：
- 协调各个管理器
- 处理游戏主循环
- 管理玩家对象
- 处理游戏状态（分数、时间、结束条件）

## 重构的优势

### 1. **职责分离**
- 每个类都有明确的单一职责
- 代码结构更清晰
- 便于理解和维护

### 2. **可扩展性**
- 需要新的实体类型？只需修改对应的管理器
- 需要新的碰撞逻辑？只需修改 CollisionManager
- 需要新的技能？只需修改 SkillManager

### 3. **可测试性**
- 每个管理器可以独立进行单元测试
- 依赖关系更清晰
- 便于模拟和测试

### 4. **代码复用**
- 管理器可以在其他游戏模式中复用
- 减少重复代码
- 提高开发效率

### 5. **内存管理**
- 使用智能指针自动管理管理器的生命周期
- 清晰的资源清理流程
- 减少内存泄漏风险

## 文件结构

```
include/Model/Managers/
├── EntityManager.h      # 实体管理器头文件
├── GeneratorManager.h   # 生成器管理器头文件
├── CollisionManager.h   # 碰撞管理器头文件
└── SkillManager.h       # 技能管理器头文件

src/Model/Managers/
├── EntityManager.cpp      # 实体管理器实现
├── GeneratorManager.cpp   # 生成器管理器实现
├── CollisionManager.cpp   # 碰撞管理器实现
└── SkillManager.cpp       # 技能管理器实现
```

## 使用示例

```cpp
// 重构后的 GameModel 使用方式
void GameModel::update(float delta_time) {
    // 使用生成器管理器更新实体生成
    generator_manager->update(delta_time);
    
    // 使用实体管理器更新所有实体
    entity_manager->updateAll(delta_time);
    
    // 更新玩家
    player->update(delta_time);
    
    // 使用碰撞管理器检查碰撞
    int collision_result = collision_manager->checkAllCollisions(player);
    
    // 处理碰撞结果...
}
```

## 后续改进建议

1. **配置文件支持**: 将实体尺寸、生成间隔等参数移到配置文件
2. **事件系统**: 引入事件系统来进一步解耦各组件
3. **组件系统**: 考虑使用 ECS (Entity Component System) 架构
4. **状态管理**: 添加专门的游戏状态管理器
5. **资源管理**: 添加统一的资源管理器

这次重构显著提高了代码的可维护性和可扩展性，为后续的功能开发奠定了良好的基础。
